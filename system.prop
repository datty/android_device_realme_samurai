# BPF: samurai ships a 4.14 kernel, but Android 16 (V+/25Q2) mainline networking
# (netbpfload + netd BpfHandler) hard-gate on kernel >= 4.19 / 5.4 and abort
# ("V+ platform with kernel version < 4.19.0 is unsupported"), crashlooping netd.
# The realme kernel has the required bpf feature backports, so report a new-enough
# version via AOSP's sanctioned override (bpf/headers KernelUtils.h uncachedKernelVersion).
# Value matches the proven oneplus/sm8150-common reference (same 4.14.357 kernel),
# which loads the full program set; a lower value would skip programs gated 5.4-5.10.
ro.bpf.kver_override=5.10.239

# Art
dalvik.vm.dex2oat64.enabled=true
dalvik.vm.boot-dex2oat-cpu-set=0,1,2,3,4,5,6,7
dalvik.vm.boot-dex2oat-threads=8
dalvik.vm.dex2oat-cpu-set=0,1,2,3,4,5,6,7
dalvik.vm.dex2oat-filter=everything
dalvik.vm.dex2oat-threads=8
dalvik.vm.image-dex2oat-cpu-set=0,1,2,3,4,5,6,7
dalvik.vm.image-dex2oat-filter=everything
dalvik.vm.image-dex2oat-threads=8

# Audio
ro.audio.monitorRotation=true
ro.config.media_vol_steps=25
ro.config.vc_call_vol_steps=7

# Bluetooth
bt.max.hfpclient.connections=1
ro.bluetooth.library_name=libbluetooth.so
qcom.bluetooth.soc=cherokee
vendor.bluetooth.soc=cherokee

# Camera
ro.camera.hfr.enable=1
ro.camera.relight.enable=0
ro.camera.attr.detect.enable=1
ro.camera.temperature.limit=470
# Do NOT set ro.oplus.system.camera.flashlight=com.oppo.motor.flashlight —
# that package is for pop-up motor devices (e.g. Realme X2). X2 Pro (samurai)
# has a fixed front camera; motor flashlight breaks QS torch / brightness path.
# Standard CameraManager torch API is used instead.
ro.oplus.camera.video_beauty.prefix=oppo.video.beauty.
ro.oplus.camera.video.beauty.switch=oppo.switch.video.beauty
persist.vendor.camera.light_ae.enable=1
ro.oplus.system.camera.name=com.oplus.camera

# Charger
ro.charger.enable_suspend=true

# Data modules
persist.vendor.data.profile_update=true
persist.vendor.data.mode=concurrent

# Fingerprint
vendor.flash.locked=0

# FUSE
persist.sys.fuse.passthrough.enable=true

# Graphics
debug.sf.auto_latch_unsignaled=0
debug.sf.disable_client_composition_cache=1
debug.sf.enable_hwc_vds=1
debug.sf.enable_transaction_tracing=false
debug.sf.enable_gl_backpressure=false
debug.sf.latch_unsignaled=1
dev.pm.dyn_samplingrate=1
persist.front.skipdisplay.enable=1
ro.vendor.display.sensortype=2
ro.vendor.display.ad.sdr_calib_data=/vendor/etc/OPPO_OLED_AD_calib.cfg

# HWUI
debug.hwui.use_hint_manager=true
debug.hwui.target_cpu_time_percent=30
ro.hwui.render_ahead=3

# IOP
vendor.iop.enable_prefetch_ofr=1
vendor.iop.enable_uxe=1

# IWLAN
persist.data.iwlan.enable=true
persist.data.iwlan.rekey=4294967295

# Media
media.settings.xml=/vendor/etc/media_profiles_vendor.xml
media.stagefright.thumbnail.prefer_hw_codecs=true
persist.mm.enable.prefetch=true
ro.media.recorder-max-base-layer-fps=60
vendor.media.vpp.enable=true

# Memory optimizations
ro.vendor.qti.sys.fw.bservice_enable=true

# Netflix custom property
ro.netflix.bsp_rev=Q855-16947-1

# NFC
ro.nfc.port=I2C

# RCS and IMS
persist.dbg.ims_volte_enable=1
persist.dbg.wfc_avail_ovr=1
persist.dbg.volte_avail_ovr=1
persist.dbg.vt_avail_ovr=1
persist.rcs.supported=1

# RIL
# persist.vendor.radio.* lives in vendor.prop only (partition ownership)
persist.radio.hvolte.enable=1
persist.radio.custom_exp_ecc=1
persist.vendor.radio.data_con_rprt=1
persist.vendor.radio.process_sups_ind=1
ril.subscription.types=NV,RUIM
ro.telephony.default_network=22,22
telephony.lteOnCdmaDevice=1

# Rmnet data
persist.rmnet.data.enable=true
persist.data.wda.enable=true
persist.data.df.dl_mode=5
persist.data.df.ul_mode=5
persist.data.df.agg.dl_pkt=10
persist.data.df.agg.dl_size=4096
persist.data.df.mux_count=8
persist.data.df.iwlan_mux=9
persist.data.df.dev_name=rmnet_usb0

# Sensors
persist.vendor.sensors.enable.mag_filter=true
# Wake-up sensors (tilt / pickup / proximity gestures). false breaks
# lift-to-wake and RealmeParts / OplusDoze pickup while screen is off.
ro.qti.sensors.wu=true

# SSR
persist.vendor.ssr.enable_ramdumps=0
persist.vendor.ssr.restart_level=ALL_ENABLE

# Storage
persist.sys.binary_xml=false

# Time
persist.vendor.delta_time.enable=true

# WiFi
config.disable_rtt=true
wifi.supplicant_scan_interval=120

# Verity
ro.apk_verity.mode=2
